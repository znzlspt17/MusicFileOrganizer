<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="MusicFileOrganizer.UI.DestinationSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:MusicFileOrganizer.UI"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button x:Name="DestinationSelectButton"
                Grid.Column="0"
                Click="OnClickDestination"
                Width="32"
                Height="32"
                Padding="0"
                BorderThickness="0"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <FontIcon Glyph="&#xE8B7;"
                      FontSize="16"
                      VerticalAlignment="Center"
                      HorizontalAlignment="Center" />
        </Button>

        <TextBox x:Name="DestinationPathBox"
                 Grid.Column="1"
                 AllowFocusWhenDisabled="True"
                 IsReadOnly="True"
                 Text=""
                 FontFamily="Consolas"
                 FontSize="16"
                 BorderThickness="0"
                 HorizontalAlignment="Stretch"
                 TextWrapping="NoWrap"
                 VerticalContentAlignment="Center"
                 Height="32"
                 Tapped="OnTappedDestination" />
    </Grid>

</UserControl>
